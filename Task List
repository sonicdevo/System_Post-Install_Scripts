# For Ubuntu Desktop 23.04
# Link Google and Ubuntu SSO accounts
# Add Network Shares to Files app [NAS, Router-USB]

# Update and upgrade: https://askubuntu.com/questions/733434/one-single-command-to-update-everything-in-ubuntu
sudo apt update -y && sudo apt full-upgrade -y && sudo apt autoremove -y && sudo apt clean -y && sudo apt autoclean -y

# Installs Timeshift
sudo apt install -y timeshift
# Next, run Timeshift GUI and setup scheduled and/or on-demand backups. Create an initial backup.

# Install Flatpak and GNOME Software "Store"
sudo apt install -y gnome-software gnome-software-plugin-flatpak flatpak
# Add Flatpak repo
flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo

# Install GNOME's extension manager
sudo apt install gnome-shell-extension-manager
# Install and configure GNOME extensions: OpenWeather, Power Profile Switcher


flatpak install com.microsoft.Edge
# Add Edge Apps: iCloud, Heimdall

#Enable minimize on click
gsettings set org.gnome.shell.extensions.dash-to-dock click-action 'minimize'

#
# BACKUP via Timeshift
#

sudo apt install -y htop neofetch

# Install via GNOME Software (Flatpak): Discord
